# =================
# == SKHD config ==
# =================

#
# Quick launch app
#
# cmd - 0x24 : wezterm

#
# Yabai & SKHD lifecycle
#
ctrl + shift + alt + cmd - t : skhd --restart-service
ctrl + shift + alt + cmd - s : yabai --start-service
ctrl + shift + alt + cmd - q : yabai --stop-service
ctrl + shift + alt + cmd - r : yabai --restart-service

#
# Desktop (Space)
#

# focus # can't do without disableing SIP
# shift + alt + cmd - j : yabai -m space --focus next
# shift + alt + cmd - l : yabai -m space --focus next
# shift + alt + cmd - k : yabai -m space --focus prev
# shift + alt + cmd - h : yabai -m space --focus prev

# shift + alt + cmd - 1 : yabai -m space --focus 1
# shift + alt + cmd - 2 : yabai -m space --focus 2
# shift + alt + cmd - 3 : yabai -m space --focus 3
# shift + alt + cmd - 4 : yabai -m space --focus 4

# send
ctrl + shift + cmd - j : yabai -m window --space next; yabai -m space --focus next
ctrl + shift + cmd - l : yabai -m window --space next; yabai -m space --focus next
ctrl + shift + cmd - k : yabai -m window --space prev; yabai -m space --focus prev
ctrl + shift + cmd - h : yabai -m window --space prev; yabai -m space --focus prev
# ctrl + shift + cmd - j : yabai -m window --space next
# ctrl + shift + cmd - l : yabai -m window --space next
# ctrl + shift + cmd - k : yabai -m window --space prev
# ctrl + shift + cmd - h : yabai -m window --space prev


#
# Monitor (Display)
#

# focus
alt + cmd - j : yabai -m display --focus next
alt + cmd - l : yabai -m display --focus next
alt + cmd - k : yabai -m display --focus prev
alt + cmd - h : yabai -m display --focus prev

alt + cmd - 1 : yabai -m display --focus 1
alt + cmd - 2 : yabai -m display --focus 2
alt + cmd - 3 : yabai -m display --focus 3


# send
# not working since macOS 14.5
# shift + alt + cmd - j : yabai -m window --display next; yabai -m display --focus next
# shift + alt + cmd - l : yabai -m window --display next; yabai -m display --focus next
# shift + alt + cmd - k : yabai -m window --display prev; yabai -m display --focus prev
# shift + alt + cmd - h : yabai -m window --display prev; yabai -m display --focus prev

# ctrl + alt + cmd - 1 : yabai -m window --display 1; yabai -m display --focus 1
# ctrl + alt + cmd - 2 : yabai -m window --display 2; yabai -m display --focus 2
# ctrl + alt + cmd - 3 : yabai -m window --display 3; yabai -m display --focus 3

